function cwiczenie17() {

// cw17

    let bound = 5;
    let value = 0;
    let counter = 0;

    while (value < bound) {
        (counter += value);
        value++
    }
    console.log(counter);
}

